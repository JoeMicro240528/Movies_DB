
const SectionHeding = ({ children }:{ children: React.ReactNode} ) => {
  return (
   <h4 className="text-2xl font-bold text-gray-200 z-20 relative text-left mx-20"> 
       {children}
   </h4>
  )
}

export default SectionHeding